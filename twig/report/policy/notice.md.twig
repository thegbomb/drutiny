#### {{ result.policy.title }}

{% with result.tokens %}
{{ include(template_from_string(result.policy.description | mustache2twig, "description field on notice.md.twig")) }}

{{ include(template_from_string(result.policy.success | mustache2twig, "success field on notice.md.twig")) }}
{% endwith %}
