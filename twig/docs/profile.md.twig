# {{ title }}
{{ description }}

## Use this profile

```
drutiny profile:run {{ name }} <target>
```

{% if policies %}
## Policies

Policy | Severity | Overrides
------ | -------- | ---------
{% for key in policies|keys %}
{{ key }} | {{ policies[key].severity is defined ? policies[key].severity }} | {{ policies[key].parameters is defined ? policies[key].parameters|json_encode()|raw }}
{% endfor %}
{% endif %}

{% if include is defined and include is iterable %}
## Inherits
This profile inherits policies from:
{% for profile in include %}
- {{ profile }}
{% endfor %}
{% endif %}
